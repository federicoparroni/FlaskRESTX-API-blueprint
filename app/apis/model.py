from .namespace_root import api
from flask_restx import fields

# define api models here

# UserModel = api.model('User', {
#     'id': fields.Integer(description='The user unique identifier'),
#     'username': fields.String(description='The username'),
# })
